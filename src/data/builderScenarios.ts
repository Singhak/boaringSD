import { BuilderScenario } from "@/types";

// Each boss starts from the previous level's stack (or the learner's own passing
// design for that level) and adds one new constraint. Numbers are tuned against
// simulateTopology() in src/lib/builderScore.ts; see builderScore.test.ts.

type Start = Pick<BuilderScenario, "startingNodes" | "startingEdges">;

const USERS = { id: "users", label: "Users", type: "client" as const, x: 0, y: 160 };
const LB = { id: "lb", label: "Load Balancer", type: "load_balancer" as const, x: 220, y: 160 };
const DB = { id: "db", label: "Postgres Primary", type: "database" as const, x: 700, y: 220 };
const server = (n: number, y: number) => ({
  id: `server-${n}`,
  label: `API Server ${n}`,
  type: "server" as const,
  x: 440,
  y,
});
const replica = (n: number, y: number) => ({
  id: `replica-${n}`,
  label: `Read Replica ${n}`,
  type: "replica" as const,
  x: 920,
  y,
});

function fleet(count: number) {
  const nodes = Array.from({ length: count }, (_, i) => server(i + 1, 40 + i * 110));
  return nodes;
}

function lbStack(serverCount: number, extras: Start["startingNodes"] = [], extraEdges: Start["startingEdges"] = []): Start {
  const servers = fleet(serverCount);
  return {
    startingNodes: [USERS, LB, ...servers, DB, ...extras],
    startingEdges: [
      { source: "users", target: "lb" },
      ...servers.map((s) => ({ source: "lb", target: s.id })),
      ...servers.map((s) => ({ source: s.id, target: "db" })),
      ...extraEdges,
    ],
  };
}

const TWO_REPLICAS = [replica(1, 120), replica(2, 320)];
const REPLICA_EDGES = [
  { source: "db", target: "replica-1" },
  { source: "db", target: "replica-2" },
];
const CACHE = { id: "cache", label: "Redis Cache", type: "cache" as const, x: 700, y: 40 };
const CDN = { id: "cdn", label: "Edge CDN", type: "cdn" as const, x: 0, y: 20 };

export const BUILDER_SCENARIOS: BuilderScenario[] = [
  {
    id: "boss-scale",
    patternId: "horizontal-scaling",
    title: "Launch Day Spike",
    userScale: "200k signups in one afternoon",
    trafficRps: 12000,
    trafficPattern: "Steady 12,000 req/s after a front-page launch",
    failureCondition: "One API server receives every request",
    objective: "Keep every API server below 60% CPU at 12,000 req/s",
    winCondition: "At least 2 servers share traffic, no node overloaded, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.9,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 60, minServers: 2 },
    requiredComponents: ["load_balancer", "database"],
    startingNodes: [USERS, { ...server(1, 160) }, DB],
    startingEdges: [
      { source: "users", target: "server-1" },
      { source: "server-1", target: "db" },
    ],
    explain: {
      question: "Your design passes. When would it still fail?",
      options: [
        {
          id: "e-db",
          label: "When traffic grows until the single database saturates",
          isCorrect: true,
          explanation: "More app servers mean more queries to the same database. Scaling one tier moves the bottleneck to the next.",
        },
        {
          id: "e-never",
          label: "Never; horizontal scaling removes all limits",
          isCorrect: false,
          explanation: "Every shared dependency still has a limit. Here, that is the database.",
        },
        {
          id: "e-lb",
          label: "When users log in from mobile",
          isCorrect: false,
          explanation: "The client type does not change how load is spread.",
        },
      ],
    },
    hints: [
      "Look at the CPU on each server. How is the traffic split between them?",
      "Extra servers only help if something spreads requests across them.",
      "Add a second API server and a load balancer between Users and the servers.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-lb",
    patternId: "load-balancing",
    title: "Server Down at Peak",
    userScale: "Same 12,000 req/s, and one server will crash",
    trafficRps: 12000,
    trafficPattern: "12,000 req/s while the last server in the fleet crashes",
    failureCondition: "The stress test kills one API server mid-spike",
    objective: "Keep surviving servers below 60% CPU after losing one",
    winCondition: "Surviving servers below 60% CPU, no node overloaded, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.9,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: true,
    targets: { maxServerCpu: 60 },
    requiredComponents: ["load_balancer", "database"],
    inheritsFrom: "boss-scale",
    ...lbStack(2),
    explain: {
      question: "How does the load balancer know a server has crashed?",
      options: [
        {
          id: "e-health",
          label: "It sends regular health-check requests and removes servers that stop answering",
          isCorrect: true,
          explanation: "Health checks let the balancer take a dead server out of rotation within seconds.",
        },
        {
          id: "e-dns",
          label: "DNS tells it",
          isCorrect: false,
          explanation: "DNS does not track server health, and changes take minutes to spread.",
        },
        {
          id: "e-client",
          label: "Clients report errors to it",
          isCorrect: false,
          explanation: "Clients see errors, but the balancer cannot rely on them to detect failures.",
        },
      ],
    },
    hints: [
      "If one server disappears, who takes over its share of the traffic?",
      "Plan capacity for N+1: enough servers that losing one still leaves headroom.",
      "Add a third API server behind the load balancer.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-replicas",
    patternId: "read-replicas",
    title: "Feed Reads Flood the Database",
    userScale: "24,000 req/s of personalized feeds",
    trafficRps: 24000,
    trafficPattern: "95% reads; each feed is personalized, so a cache only hits 20%",
    failureCondition: "The primary database handles every read and write",
    objective: "Keep every database node below 70% CPU",
    winCondition: "All database nodes below 70% CPU, servers below 70%, score ≥ 70",
    readRatio: 0.95,
    cacheHitRate: 0.2,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 70, maxDbCpu: 70 },
    requiredComponents: ["load_balancer", "database", "replica"],
    inheritsFrom: "boss-lb",
    ...lbStack(3),
    explain: {
      question: "A user posts and immediately reloads, but the post is missing. Why?",
      options: [
        {
          id: "e-lag",
          label: "The read went to a replica that has not received the write yet",
          isCorrect: true,
          explanation: "Asynchronous replication lags slightly. Read-your-own-writes routing fixes it for the author.",
        },
        {
          id: "e-lb",
          label: "The load balancer dropped the write",
          isCorrect: false,
          explanation: "The write succeeded on the primary; the replica just has not caught up.",
        },
        {
          id: "e-cache",
          label: "The CDN cached the old feed",
          isCorrect: false,
          explanation: "There is no CDN in this design. The lag is between primary and replica.",
        },
      ],
    },
    hints: [
      "Which node is red during the stress test? What kind of queries does it serve?",
      "95% of queries are reads. Could copies of the database serve them?",
      "Add read replicas connected to the primary. One may not be enough.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-cache",
    patternId: "caching",
    title: "Celebrity Post",
    userScale: "30,000 req/s for the same few posts",
    trafficRps: 30000,
    trafficPattern: "95% reads of a handful of hot posts; a cache hits 95%",
    failureCondition: "Replicas read identical rows from disk over and over",
    objective: "Database nodes below 30% CPU and p95 latency below 80ms",
    winCondition: "Cache connected, DB nodes < 30%, servers < 75%, latency < 80ms, score ≥ 70",
    readRatio: 0.95,
    cacheHitRate: 0.95,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 75, maxDbCpu: 30, maxLatencyMs: 80 },
    requiredComponents: ["load_balancer", "database", "cache"],
    inheritsFrom: "boss-replicas",
    ...lbStack(3, TWO_REPLICAS, REPLICA_EDGES),
    explain: {
      question: "The hot post's cache entry expires and 10,000 requests miss at once. What protects the database?",
      options: [
        {
          id: "e-lock",
          label: "Let one request rebuild the entry while the others wait or get the old value",
          isCorrect: true,
          explanation: "A lock (single-flight) or early refresh turns 10,000 database queries into one.",
        },
        {
          id: "e-nottl",
          label: "Never expire cache entries",
          isCorrect: false,
          explanation: "Entries would go stale forever and memory would fill up.",
        },
        {
          id: "e-timeout",
          label: "Raise the database timeout",
          isCorrect: false,
          explanation: "Longer timeouts let the pile-up grow.",
        },
      ],
    },
    hints: [
      "Most reads ask for the same few rows. Do they need to reach a disk each time?",
      "An in-memory layer can answer repeated reads before the database sees them. Also check your server CPU.",
      "Add a cache connected to the API servers, and add a fourth server if CPU is above target.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-cdn",
    patternId: "cdn-edge",
    title: "Global Launch",
    userScale: "30,000 req/s from four continents",
    trafficRps: 30000,
    trafficPattern: "Half the traffic is images and scripts; users are worldwide",
    failureCondition: "Every asset crosses an ocean from one region",
    objective: "p95 latency below 150ms and servers below 60% CPU",
    winCondition: "CDN connected, latency < 150ms, servers < 60%, score ≥ 70",
    readRatio: 0.95,
    cacheHitRate: 0.95,
    staticAssetShare: 0.5,
    globalUsers: true,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 60, maxLatencyMs: 150 },
    requiredComponents: ["load_balancer", "database", "cache", "cdn"],
    inheritsFrom: "boss-cache",
    ...lbStack(
      4,
      [CACHE, ...TWO_REPLICAS],
      [
        ...REPLICA_EDGES,
        ...[1, 2, 3, 4].map((n) => ({ source: `server-${n}`, target: "cache" })),
      ]
    ),
    explain: {
      question: "You deploy a fix to app.js, but users still get the old file from the edge. What prevents this?",
      options: [
        {
          id: "e-hash",
          label: "Put a content hash in asset filenames so each build has new URLs",
          isCorrect: true,
          explanation: "New content, new URL: edges fetch it immediately and old files can stay cached.",
        },
        {
          id: "e-off",
          label: "Turn off CDN caching for scripts",
          isCorrect: false,
          explanation: "Every script request would return to the origin, undoing the fix.",
        },
        {
          id: "e-refresh",
          label: "Ask users to hard-refresh",
          isCorrect: false,
          explanation: "You cannot depend on users clearing caches.",
        },
      ],
    },
    hints: [
      "Latency is high even though servers are not overloaded. Where is the time going?",
      "Static files could be served from somewhere closer to users.",
      "Add a CDN and connect Users to it.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-queue",
    patternId: "async-queues",
    title: "Flash Sale Checkout",
    userScale: "30,000 req/s during a flash sale",
    trafficRps: 30000,
    trafficPattern: "Every checkout calls a slow payment API and sends an email",
    failureCondition: "Request threads wait on slow third-party calls",
    objective: "p95 latency below 200ms and servers below 60% CPU",
    winCondition: "Queue connected, latency < 200ms, servers < 60%, score ≥ 70",
    readRatio: 0.95,
    cacheHitRate: 0.95,
    staticAssetShare: 0.5,
    globalUsers: true,
    slowDownstream: true,
    killOneServer: false,
    targets: { maxServerCpu: 60, maxLatencyMs: 200 },
    requiredComponents: ["load_balancer", "database", "cache", "cdn", "queue"],
    inheritsFrom: "boss-cdn",
    ...lbStack(
      4,
      [CACHE, CDN, ...TWO_REPLICAS],
      [
        { source: "users", target: "cdn" },
        ...REPLICA_EDGES,
        ...[1, 2, 3, 4].map((n) => ({ source: `server-${n}`, target: "cache" })),
      ]
    ),
    explain: {
      question: "A worker crashes after charging a card but before acknowledging the message. What stops a double charge on redelivery?",
      options: [
        {
          id: "e-idem",
          label: "An idempotency key, so the same charge request is only applied once",
          isCorrect: true,
          explanation: "Queues deliver at least once; idempotent handlers make retries safe.",
        },
        {
          id: "e-noretry",
          label: "Disable redelivery",
          isCorrect: false,
          explanation: "Then crashed work is lost entirely.",
        },
        {
          id: "e-sync",
          label: "Charge the card inside the web request instead",
          isCorrect: false,
          explanation: "That brings back blocked threads and still needs retries.",
        },
      ],
    },
    hints: [
      "Servers are busy, but mostly waiting. What are they waiting for?",
      "Work the user does not need to wait for can happen after the response.",
      "Add a message queue connected to the API servers.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-sharding",
    patternId: "sharding",
    title: "Tenant Hotspot",
    userScale: "42,000 req/s across tenants",
    trafficRps: 42000,
    trafficPattern: "60% of requests hit the same tenant's orders table",
    failureCondition: "One shard carries almost every write for the hottest tenant",
    objective: "Spread the hotspot and keep database shards below 70% CPU",
    winCondition: "At least two shards balanced, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.5,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxDbCpu: 70, maxServerCpu: 70 },
    requiredComponents: ["load_balancer", "database"],
    inheritsFrom: "boss-queue",
    ...lbStack(
      4,
      [
        { id: "db-shard-a", label: "Orders Shard A", type: "database" as const, x: 780, y: 90 },
        { id: "db-shard-b", label: "Orders Shard B", type: "database" as const, x: 820, y: 300 },
      ],
      [
        { source: "server-1", target: "db-shard-a" },
        { source: "server-2", target: "db-shard-a" },
        { source: "server-3", target: "db-shard-b" },
        { source: "server-4", target: "db-shard-b" },
      ]
    ),
    explain: {
      question: "The hottest tenant still dominates one partition. What is the real design issue?",
      options: [
        {
          id: "e-skew",
          label: "The key distribution is skewed, so one shard holds most writes",
          isCorrect: true,
          explanation: "Even with multiple shards, data placement matters. A hot tenant or key can still saturate one partition.",
        },
        {
          id: "e-mem",
          label: "The API servers have too much memory",
          isCorrect: false,
          explanation: "The hotspot is at the data layer, not the app tier.",
        },
        {
          id: "e-cdn",
          label: "A CDN is serving stale orders",
          isCorrect: false,
          explanation: "CDN caching is not relevant to a transactional orders table.",
        },
      ],
    },
    hints: [
      "Are writes and reads spread evenly across all database nodes?",
      "One tenant or key can still create a hotspot inside a perfectly valid shard design.",
      "Consider tenant-aware key routing or a more balanced partitioning scheme.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-consistency",
    patternId: "consistency",
    title: "Stale After Update",
    userScale: "25,000 req/s with author refreshes",
    trafficRps: 25000,
    trafficPattern: "Users update a profile and read it back immediately while replicas lag behind",
    failureCondition: "Reads hit stale followers and the author sees the old record",
    objective: "Keep user-visible reads fresh and all database nodes under 70% CPU",
    winCondition: "Read-your-own-writes path configured, latency < 150ms, score ≥ 70",
    readRatio: 0.85,
    cacheHitRate: 0.75,
    staticAssetShare: 0,
    globalUsers: true,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxDbCpu: 70, maxServerCpu: 70, maxLatencyMs: 150 },
    requiredComponents: ["load_balancer", "database", "replica"],
    inheritsFrom: "boss-sharding",
    ...lbStack(
      4,
      [
        { id: "replica-async", label: "Async Replica", type: "replica" as const, x: 980, y: 100 },
        { id: "replica-async-2", label: "Follower Replica", type: "replica" as const, x: 980, y: 300 },
      ],
      [
        { source: "db", target: "replica-async" },
        { source: "db", target: "replica-async-2" },
        { source: "server-1", target: "db" },
        { source: "server-2", target: "db" },
        { source: "server-3", target: "db" },
        { source: "server-4", target: "db" },
      ]
    ),
    explain: {
      question: "The author updates the profile but immediately sees the old record. What is the correct design fix?",
      options: [
        {
          id: "e-readfresh",
          label: "Route that user's reads to the primary or a session-aware fresh source until replication catches up",
          isCorrect: true,
          explanation: "Read-your-own-writes keeps the author consistent without disabling replication for everyone.",
        },
        {
          id: "e-disable",
          label: "Disable replicas so all reads are fresh",
          isCorrect: false,
          explanation: "That restores freshness by bringing back the original database bottleneck.",
        },
        {
          id: "e-timeout",
          label: "Increase the timeout and retry later",
          isCorrect: false,
          explanation: "Retries do not fix stale reads; they just delay the user experience.",
        },
      ],
    },
    hints: [
      "The author has a freshness requirement, even when the rest of the system is eventually consistent.",
      "Which database node should that user's reads touch first after a write?",
      "Add a read-your-own-writes path for the active user without turning off replication.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-rate-limiting",
    patternId: "rate-limiting",
    title: "Abusive Client Flood",
    userScale: "40,000 req/s with rogue client burst",
    trafficRps: 40000,
    trafficPattern: "One rogue tenant floods API endpoints while normal users experience 504 timeouts",
    failureCondition: "API edge has no rate limiting; server CPU spikes to 95%",
    objective: "Protect fleet capacity by adding a rate-limiting cache layer at the edge",
    winCondition: "Rate-limiting cache connected, servers under 70% CPU, score ≥ 70",
    readRatio: 0.8,
    cacheHitRate: 0.85,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 70, maxDbCpu: 70 },
    requiredComponents: ["load_balancer", "database", "cache"],
    inheritsFrom: "boss-consistency",
    ...lbStack(3),
    explain: {
      question: "One client sends 20,000 req/s. What prevents them from starving other users?",
      options: [
        {
          id: "e-ratelimit",
          label: "Per-tenant token-bucket rate limiting at the gateway tier backed by fast in-memory storage",
          isCorrect: true,
          explanation: "Edge rate limiting throttles abusive keys with HTTP 429 before requests reach backend compute.",
        },
        {
          id: "e-scale",
          label: "Infinitely provision more compute servers to absorb any volume of abuse",
          isCorrect: false,
          explanation: "Unbounded scaling is economically impossible and moves the failure downstream to databases.",
        },
        {
          id: "e-timeout",
          label: "Increase server request timeouts",
          isCorrect: false,
          explanation: "Longer timeouts hold connection threads longer, worsening thread exhaustion.",
        },
      ],
    },
    hints: [
      "Where should rate limit counters be maintained for fast sub-millisecond lookups?",
      "Add a Redis Cache connected to the application fleet to track tenant token buckets.",
      "Connect an in-memory cache to maintain high-speed token buckets without hitting disks.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-circuit-breaker",
    patternId: "circuit-breaker",
    title: "Downstream Payment Freeze",
    userScale: "30,000 req/s with hung payment dependency",
    trafficRps: 30000,
    trafficPattern: "Synchronous payment processing freezes; all worker threads lock up waiting on timeouts",
    failureCondition: "Threads exhaust waiting on slow downstream; storefront collapses",
    objective: "Decouple synchronous dependency calls with an asynchronous queue",
    winCondition: "Queue connected, worker threads protected, latency < 200ms, score ≥ 70",
    readRatio: 0.7,
    cacheHitRate: 0.6,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: true,
    killOneServer: false,
    targets: { maxServerCpu: 70, maxDbCpu: 70 },
    requiredComponents: ["load_balancer", "database", "queue"],
    inheritsFrom: "boss-rate-limiting",
    ...lbStack(3),
    explain: {
      question: "A downstream service is failing 100% of calls. What should the client-side caller do?",
      options: [
        {
          id: "e-trip",
          label: "Trip the circuit to Open, fail fast immediately, and schedule canary probes in Half-Open state",
          isCorrect: true,
          explanation: "Failing fast stops thread exhaustion and gives the downstream service breathing room to recover.",
        },
        {
          id: "e-retryharder",
          label: "Retry every failed call 10 times with zero delay",
          isCorrect: false,
          explanation: "Aggressive retries create an amplified DDoS storm on the struggling dependency.",
        },
        {
          id: "e-ignore",
          label: "Ignore the failures and keep waiting 60s on every thread",
          isCorrect: false,
          explanation: "Waiting on dead calls locks all server threads and brings down unrelated features.",
        },
      ],
    },
    hints: [
      "Decouple synchronous blocking checkout calls by adding a Message Queue.",
      "Ensure workers process payments asynchronously while the web tier responds immediately.",
      "Ensure caller threads return fallback responses when the circuit trips open.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-connection-pooling",
    patternId: "connection-pooling",
    title: "Database Socket Starvation",
    userScale: "45,000 req/s across high-concurrency app fleet",
    trafficRps: 45000,
    trafficPattern: "Every request attempts to open a separate TLS connection directly to Postgres",
    failureCondition: "Database max_connections exceeded; connection handshakes throttle CPU",
    objective: "Conserve database connections by caching hot reads and adding read replicas",
    winCondition: "Cache and read replicas in place, DB CPU < 70%, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.8,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxDbCpu: 70, maxServerCpu: 70 },
    requiredComponents: ["load_balancer", "database", "cache", "replica"],
    inheritsFrom: "boss-circuit-breaker",
    ...lbStack(3),
    explain: {
      question: "Why should database connection pools be bounded to a modest number of connections?",
      options: [
        {
          id: "e-cores",
          label: "A database can only execute as many parallel queries as available CPU cores and disk channels",
          isCorrect: true,
          explanation: "Excess connections cause context switching and memory allocation overhead without increasing execution throughput.",
        },
        {
          id: "e-cables",
          label: "Ethernet switches only support 50 TCP streams simultaneously",
          isCorrect: false,
          explanation: "Networking hardware easily handles millions of packets; the limit is CPU thread scheduling on the DB server.",
        },
        {
          id: "e-ports",
          label: "PostgreSQL port 5432 closes if more than 100 queries arrive",
          isCorrect: false,
          explanation: "The port stays open, but excessive active backend processes exhaust server RAM and CPU time.",
        },
      ],
    },
    hints: [
      "Shield the database from connection exhaustion using an in-memory Cache and Read Replicas.",
      "Distribute the read workload so the primary DB handles only write traffic.",
      "Multiplex application traffic across bounded connection pools to prevent session exhaustion.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-backpressure",
    patternId: "backpressure",
    title: "Worker Ingestion Overload",
    userScale: "50,000 tasks/s submitted during flash import",
    trafficRps: 50000,
    trafficPattern: "Producers enqueue tasks 5x faster than downstream workers can process them",
    failureCondition: "Unbounded buffer growth threatens process memory with fatal OOM",
    objective: "Implement a bounded queue with producer throttling to stabilize memory",
    winCondition: "Queue and Cache connected, server CPU under 70%, score ≥ 70",
    readRatio: 0.6,
    cacheHitRate: 0.7,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: true,
    killOneServer: false,
    targets: { maxServerCpu: 70, maxDbCpu: 70 },
    requiredComponents: ["load_balancer", "database", "queue", "cache"],
    inheritsFrom: "boss-connection-pooling",
    ...lbStack(3),
    explain: {
      question: "What is the primary purpose of applying backpressure in high-throughput pipelines?",
      options: [
        {
          id: "e-oom",
          label: "To force upstream producers to slow down when downstream queues reach safe buffer thresholds",
          isCorrect: true,
          explanation: "Backpressure signals saturation before buffers overflow and trigger catastrophic Out-Of-Memory crashes.",
        },
        {
          id: "e-encrypt",
          label: "To encrypt messages before they reach workers",
          isCorrect: false,
          explanation: "Backpressure is a flow-control mechanism, unrelated to data encryption.",
        },
        {
          id: "e-compress",
          label: "To compress SQL statements before execution",
          isCorrect: false,
          explanation: "Flow control manages processing rate, not query payload format.",
        },
      ],
    },
    hints: [
      "Equip your architecture with a Message Queue and a Cache layer to pace ingest and buffer work.",
      "Add a Queue to buffer incoming task surges and isolate worker processing.",
      "Configure producer throttling when the buffer watermark reaches safety bounds.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-idempotency",
    patternId: "idempotency",
    title: "Duplicate Mutation Storm",
    userScale: "35,000 req/s with aggressive client network retries",
    trafficRps: 35000,
    trafficPattern: "Unstable mobile connections cause clients to retry payment POST requests 3-5 times",
    failureCondition: "Database executes duplicate writes; duplicate charges occur",
    objective: "Protect mutation paths by introducing a deduplicating Cache for idempotency keys",
    winCondition: "Cache and DB connected, database CPU < 70%, score ≥ 70",
    readRatio: 0.7,
    cacheHitRate: 0.85,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxDbCpu: 70, maxServerCpu: 70 },
    requiredComponents: ["load_balancer", "database", "cache"],
    inheritsFrom: "boss-backpressure",
    ...lbStack(3),
    explain: {
      question: "How does an API guarantee safe retries when processing non-idempotent operations like charges?",
      options: [
        {
          id: "e-key",
          label: "Require an Idempotency-Key header; store the completed response atomically so retries return cached results",
          isCorrect: true,
          explanation: "An idempotency key guarantees that repeated requests return the same recorded response without re-executing business logic.",
        },
        {
          id: "e-ip",
          label: "Block the user's IP for 10 minutes after any payment attempt",
          isCorrect: false,
          explanation: "Blocking users after a payment ruins customer experience and breaks legitimate workflows.",
        },
        {
          id: "e-allow",
          label: "Allow all charges and tell customers to contact customer support for refunds",
          isCorrect: false,
          explanation: "Neglecting idempotency causes widespread financial disputes and severe reputational damage.",
        },
      ],
    },
    hints: [
      "Add a fast in-memory Cache layer to record and verify unique idempotency keys.",
      "Store mutation results with unique keys to deduplicate client retries.",
      "Return cached transaction responses on duplicate submissions.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-multi-region",
    patternId: "multi-region",
    title: "Global Continental Failover",
    userScale: "80,000 req/s across North America, Europe, and Asia",
    trafficRps: 80000,
    trafficPattern: "High global traffic; single origin datacenter introduces 350ms cross-ocean latency",
    failureCondition: "High geographic latency and regional single point of failure",
    objective: "Deploy CDN Edge caching to distribute static traffic close to global users",
    winCondition: "Edge CDN deployed, latency < 100ms, database CPU < 70%, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.9,
    staticAssetShare: 0.4,
    globalUsers: true,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxLatencyMs: 100, maxServerCpu: 70, maxDbCpu: 70 },
    requiredComponents: ["load_balancer", "database", "cdn"],
    inheritsFrom: "boss-idempotency",
    ...lbStack(3),
    explain: {
      question: "What is the primary advantage of deploying Edge CDN and Multi-Region infrastructure?",
      options: [
        {
          id: "e-edge",
          label: "Terminates TLS and serves cached content close to users, cutting transatlantic latency while providing geographic failover",
          isCorrect: true,
          explanation: "Proximity reduces speed-of-light propagation delays and eliminates single-datacenter failure vulnerability.",
        },
        {
          id: "e-free",
          label: "CDN edge servers eliminate the need for databases entirely",
          isCorrect: false,
          explanation: "CDNs cache static assets and read-heavy responses, but transactional datasets still require persistent databases.",
        },
        {
          id: "e-dns",
          label: "DNS becomes obsolete when using multi-region hosting",
          isCorrect: false,
          explanation: "Multi-region setups rely extensively on Anycast and health-checked Geo-DNS routing.",
        },
      ],
    },
    hints: [
      "Connect an Edge CDN to serve assets close to global users and reduce origin transit times.",
      "Deploy CDN Edge caching to eliminate round-trip latency for global users.",
      "Configure multi-datacenter failover routes with Anycast routing.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-health-checks",
    patternId: "health-checks",
    title: "Zombie Node Detection",
    userScale: "50,000 req/s while one app server hangs on 100% of requests",
    trafficRps: 50000,
    trafficPattern: "A backend instance locks up, but without health checks traffic continues routing to the dead host",
    failureCondition: "Stress test kills one server; remaining capacity must absorb load seamlessly",
    objective: "Design an N+1 resilient server cluster with load balancing and database caching",
    winCondition: "All surviving servers under 65% CPU after node failure, score ≥ 70",
    readRatio: 0.85,
    cacheHitRate: 0.85,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: true,
    targets: { maxServerCpu: 65, minServers: 3 },
    requiredComponents: ["load_balancer", "database", "cache"],
    inheritsFrom: "boss-multi-region",
    ...lbStack(2),
    explain: {
      question: "Why should Liveness probes and Readiness probes be configured separately?",
      options: [
        {
          id: "e-probes",
          label: "Liveness restarts deadlocked processes; readiness temporarily removes overloaded nodes from traffic without killing them",
          isCorrect: true,
          explanation: "Conflating the two causes flapping: a busy node that fails a probe gets killed, making the remaining cluster even more overloaded.",
        },
        {
          id: "e-same",
          label: "There is no difference; they are identical aliases in Kubernetes",
          isCorrect: false,
          explanation: "They have completely different lifecycles and remediation triggers in container orchestrators.",
        },
        {
          id: "e-tcp",
          label: "Readiness only works over UDP while liveness requires TCP",
          isCorrect: false,
          explanation: "Both typically run as HTTP GET requests or TCP socket checks.",
        },
      ],
    },
    hints: [
      "Ensure your fleet has N+1 redundant servers (at least 3 servers) so losing one node doesn't overload the rest.",
      "Add a Cache to relieve database pressure.",
      "Separate liveness probes from readiness probes to avoid reboot storms.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-cap-pacelc",
    patternId: "cap-pacelc",
    title: "Reads Across the Ocean",
    userScale: "60,000 req/s from users on two continents",
    trafficRps: 60000,
    trafficPattern: "90% reads; every read currently crosses to the single leader",
    failureCondition: "The leader serves every read, so latency and DB CPU climb together",
    objective: "Serve latency-tolerant reads from replicas while writes stay on the leader",
    winCondition: "Replicas connected, latency < 120ms, database CPU < 60%, score ≥ 70",
    readRatio: 0.9,
    cacheHitRate: 0.8,
    staticAssetShare: 0,
    globalUsers: true,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxLatencyMs: 120, maxServerCpu: 70, maxDbCpu: 60 },
    requiredComponents: ["load_balancer", "database", "replica"],
    inheritsFrom: "boss-health-checks",
    ...lbStack(3),
    explain: {
      question: "Replica reads may be a few seconds stale. Which reads should still go to the leader?",
      options: [
        {
          id: "e-own-writes",
          label: "A user's reads of data they just changed, plus balance and stock checks",
          isCorrect: true,
          explanation: "That is the PACELC 'Else' tradeoff: fast local reads where staleness is harmless, leader reads where it isn't.",
        },
        {
          id: "e-none",
          label: "None: once replicas exist every read should use the nearest one",
          isCorrect: false,
          explanation: "An author would not see their own edit and a checkout could read a stale balance.",
        },
        {
          id: "e-all",
          label: "All of them, because any stale read is a consistency bug",
          isCorrect: false,
          explanation: "Then the replicas do nothing and every read still pays the cross-ocean round trip.",
        },
      ],
    },
    hints: [
      "Most reads can tolerate a little staleness.",
      "Add replicas hanging off the primary database.",
      "Keep read-your-own-writes and money checks on the leader.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-consensus-quorums",
    patternId: "consensus-quorums",
    title: "Survive a Node Loss",
    userScale: "20,000 req/s against a replicated metadata service",
    trafficRps: 20000,
    trafficPattern: "Steady writes; the stress test kills one server mid-run",
    failureCondition: "Two app servers cannot absorb a node loss, and the DB has no follower",
    objective: "Run N+1 servers and a replicated database that keeps a majority after one failure",
    winCondition: "At least 3 servers, a replica connected, servers < 70% after the kill, score ≥ 70",
    readRatio: 0.7,
    cacheHitRate: 0.6,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: true,
    targets: { maxServerCpu: 70, minServers: 3 },
    requiredComponents: ["load_balancer", "database", "replica"],
    inheritsFrom: "boss-cap-pacelc",
    ...lbStack(2),
    explain: {
      question: "Why run three database nodes rather than two?",
      options: [
        {
          id: "e-majority",
          label: "Three nodes keep a two-node majority after one fails; two nodes lose it",
          isCorrect: true,
          explanation: "Majority quorums need more than half the nodes. With 3 you tolerate 1 failure; with 2 you tolerate none.",
        },
        {
          id: "e-throughput",
          label: "Each extra node multiplies write throughput by the node count",
          isCorrect: false,
          explanation: "Consensus writes go through one leader and wait on a majority, so more nodes don't speed up writes.",
        },
        {
          id: "e-even",
          label: "Two nodes can't hold an election, but four nodes would be even safer",
          isCorrect: false,
          explanation: "Four nodes still tolerate only one failure (majority is 3), so the fourth node adds cost, not safety.",
        },
      ],
    },
    hints: [
      "The stress test kills one server; plan for N+1.",
      "Give the database a follower it can fail over to.",
      "An odd number of voters keeps a majority after one loss.",
    ],
    passThreshold: 70,
  },
  {
    id: "boss-storage-engines",
    patternId: "storage-engines",
    title: "Hot Lookup Table",
    userScale: "45,000 req/s of point lookups and recent-range reads",
    trafficRps: 45000,
    trafficPattern: "85% reads of a small hot set; writes append new rows",
    failureCondition: "Every lookup reaches the storage engine and the disk saturates",
    objective: "Keep database CPU under 40% and p95 latency under 90ms",
    winCondition: "Cache connected, DB < 40%, latency < 90ms, score ≥ 70",
    readRatio: 0.85,
    cacheHitRate: 0.9,
    staticAssetShare: 0,
    globalUsers: false,
    slowDownstream: false,
    killOneServer: false,
    targets: { maxServerCpu: 75, maxDbCpu: 40, maxLatencyMs: 90 },
    requiredComponents: ["load_balancer", "database", "cache"],
    inheritsFrom: "boss-consensus-quorums",
    ...lbStack(3),
    explain: {
      question: "The lookup table is write-heavy at night and read-heavy by day. Which engine choice fits the nightly load?",
      options: [
        {
          id: "e-lsm",
          label: "An LSM engine: appends are sequential, and Bloom filters keep point reads cheap",
          isCorrect: true,
          explanation: "LSM trees turn random writes into sequential flushes; Bloom filters and compaction keep reads from scanning every file.",
        },
        {
          id: "e-more-idx",
          label: "A B-tree with an index on every column so any query is fast",
          isCorrect: false,
          explanation: "Each index is another random write per insert, which is exactly what hurts at night.",
        },
        {
          id: "e-hash",
          label: "An in-memory hash map of every row, flushed to disk once a day",
          isCorrect: false,
          explanation: "The data won't fit in RAM, a crash loses a day of writes, and range scans are impossible.",
        },
      ],
    },
    hints: [
      "Most reads hit a small hot set.",
      "Put a cache between the servers and the database.",
      "Pick the storage engine for the write pattern; cache the hot reads.",
    ],
    passThreshold: 70,
  },
];

export function getBuilderScenarioById(id: string): BuilderScenario | undefined {
  return BUILDER_SCENARIOS.find((s) => s.id === id);
}

export function getAllBuilderScenarios(): BuilderScenario[] {
  return BUILDER_SCENARIOS;
}
